add a suppressed attribute variant
this will have a static encode/decode method, which will provide a regular attribute if it is not suppressed

update how stat scaling is handled, assume a single entity may have multiple stat sheets

I might need to make a server and client version of attributeHolder, since the client version holds 0 logic